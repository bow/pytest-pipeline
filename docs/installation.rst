============
Installation
============

At the command line::

    $ easy_install pytest_pipeline

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pytest_pipeline
    $ pip install pytest_pipeline